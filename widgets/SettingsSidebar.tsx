"use client";

import { twMerge } from "tailwind-merge";

export function SettingsSidebar({ ...rest }: React.HTMLAttributes<HTMLDivElement>) {
	return <div {...rest} className={twMerge(rest.className, "p-4 overflow-y-auto")}>

		
	</div>;
}
